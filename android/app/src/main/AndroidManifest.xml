<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- دسترسی به دوربین -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:label="apptest4"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!--
                مشخص می‌کند Flutter هنگام شروع برنامه
                از چه Themeای استفاده کند.
            -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- اجرای MainActivity هنگام باز شدن برنامه -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!--
            Flutter embedding version 2
            این قسمت را حذف نکن.
        -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!--
        اجازه می‌دهد Flutter فعالیت‌هایی را که
        می‌توانند متن را پردازش کنند شناسایی کند.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>